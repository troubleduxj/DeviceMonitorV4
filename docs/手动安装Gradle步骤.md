# 手动安装 Gradle 8.14.3 步骤

## 📦 您已下载：`gradle-8.14.3-bin.zip`

---

## 🎯 放置步骤（3步完成）

### 步骤 1：创建目标目录

打开 **文件资源管理器**，按以下路径创建文件夹：

```
C:\Users\您的用户名\.gradle\wrapper\dists\gradle-8.14.3-bin
```

**快速方法**：
1. 按 `Win + R` 打开运行对话框
2. 输入：`%USERPROFILE%\.gradle\wrapper\dists`
3. 点击"确定"
4. 在打开的文件夹中创建新文件夹：`gradle-8.14.3-bin`

---

### 步骤 2：创建随机子目录

在 `gradle-8.14.3-bin` 文件夹内，创建一个**随机命名**的子文件夹，例如：
- `1a2b3c4d5e6f7g8h9i0j`
- 或任意字符串，比如：`abc123`

**完整路径示例**：
```
C:\Users\您的用户名\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\
```

---

### 步骤 3：复制 ZIP 文件

将您下载的 `gradle-8.14.3-bin.zip` 复制到刚创建的子目录中：

```
C:\Users\您的用户名\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip
```

⚠️ **重要**：
- **保持 ZIP 格式**，不要解压！
- 文件名必须是 `gradle-8.14.3-bin.zip`

---

## 🚀 验证和继续构建

### 验证文件位置

确认文件路径类似：
```
C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip
```

### 重新运行构建

在 PowerShell 中执行：

```powershell
# 进入项目目录
cd D:\Cursor\Project\DeviceMonitorV2_20251013_V1\DeviceMonitorV2\mobile

# 启动构建
npm run android
```

---

## ✅ 预期行为

1. Gradle wrapper 会**自动检测**到本地的 ZIP 文件
2. **自动解压**到同一目录
3. **直接使用**，不再下载
4. 继续 Android 构建流程

解压后的目录结构：
```
gradle-8.14.3-bin\
├── abc123\
│   ├── gradle-8.14.3-bin.zip          ← 您放置的文件
│   ├── gradle-8.14.3-bin.zip.ok       ← 自动生成（验证文件）
│   └── gradle-8.14.3\                 ← 自动解压（Gradle 本体）
│       ├── bin\
│       ├── lib\
│       └── ...
```

---

## 🔧 故障排除

### 如果仍然尝试下载

1. **删除 `.gradle` 缓存**：
   ```powershell
   Remove-Item -Path "$env:USERPROFILE\.gradle\caches" -Recurse -Force -ErrorAction SilentlyContinue
   ```

2. **检查文件完整性**：
   - ZIP 文件大小应约 **103 MB**
   - 确保下载完整，没有损坏

3. **重新放置**：
   - 删除之前创建的目录
   - 重新按步骤 1-3 操作

### 如果构建仍然失败

可能需要先手动解压：

```powershell
# 在 PowerShell 中解压
Expand-Archive -Path "C:\Users\您的用户名\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip" -DestinationPath "C:\Users\您的用户名\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\"
```

---

## 📞 需要帮助？

如果以上步骤遇到问题，请：
1. 截图显示您的文件路径
2. 提供构建错误信息
3. 我会进一步协助

---

## ⏭️ 下一步

构建成功后，查看：
- [构建状态-当前进度.md](./构建状态-当前进度.md)
- [移动端快速启动指南.md](./移动端快速启动指南.md)

