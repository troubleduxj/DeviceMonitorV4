# 手动解压 Gradle 并构建

由于自动化脚本可能遇到权限或执行策略问题，请**手动执行**以下步骤：

---

## 📦 步骤 1：验证文件位置

打开 PowerShell，执行：

```powershell
Test-Path "C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip"
```

**预期输出**：`True`

如果输出 `False`，请确认文件是否真的放在了这个位置。

---

## 🔓 步骤 2：手动解压 Gradle

在 PowerShell 中执行（**复制整段**）：

```powershell
# 解压 Gradle
$zipFile = "C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip"
$destDir = "C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123"

Write-Host "开始解压 Gradle..." -ForegroundColor Yellow

Expand-Archive -Path $zipFile -DestinationPath $destDir -Force

Write-Host "创建标记文件..." -ForegroundColor Yellow
New-Item -Path "$zipFile.ok" -ItemType File -Force | Out-Null

Write-Host "完成！" -ForegroundColor Green
Write-Host ""
Write-Host "解压后的内容："
Get-ChildItem $destDir
```

**预期结果**：应该看到 `gradle-8.14.3` 文件夹和 `gradle-8.14.3-bin.zip` 文件

---

## 🚀 步骤 3：开始构建

在同一个 PowerShell 窗口中，执行：

```powershell
# 进入项目目录
cd D:\Cursor\Project\DeviceMonitorV2_20251013_V1\DeviceMonitorV2\mobile

# 清理之前的构建
Remove-Item -Path "platforms/tempPlugin" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Path "platforms/android/.gradle" -Recurse -Force -ErrorAction SilentlyContinue

# 设置环境变量
$env:GRADLE_USER_HOME = "C:\Users\duxia\.gradle"

# 开始构建
npm run android
```

---

## ⚠️ 如果解压失败

### 方法 1：使用文件资源管理器解压

1. 打开文件资源管理器
2. 导航到：`C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123`
3. 右键点击 `gradle-8.14.3-bin.zip`
4. 选择"全部提取..."
5. 解压到当前文件夹（`abc123` 文件夹内）
6. 解压后应该看到 `gradle-8.14.3` 文件夹

然后在 PowerShell 中创建标记文件：

```powershell
New-Item -Path "C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\gradle-8.14.3-bin.zip.ok" -ItemType File -Force
```

### 方法 2：使用 7-Zip 或 WinRAR

如果安装了这些工具，可以使用它们解压。

---

## ✅ 验证成功

解压成功后，目录结构应该是：

```
C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123\
├── gradle-8.14.3\              ← 解压后的文件夹
│   ├── bin\
│   │   ├── gradle.bat
│   │   └── gradle
│   ├── lib\
│   └── ...
├── gradle-8.14.3-bin.zip       ← 原始 ZIP 文件
└── gradle-8.14.3-bin.zip.ok    ← 标记文件
```

---

## 🔍 故障排除

### 问题 1：PowerShell 报"无法识别 cmdlet"

**原因**：PowerShell 版本太旧

**解决**：
- 升级到 PowerShell 5.1+ 或 PowerShell Core 7+
- 或使用文件资源管理器手动解压（方法 1）

### 问题 2：解压时提示"访问被拒绝"

**原因**：权限不足

**解决**：
1. 以管理员身份运行 PowerShell
2. 或将 ZIP 文件复制到其他位置解压后再移回

### 问题 3：解压后仍然下载

**原因**：NativeScript 可能覆盖了 Gradle 配置

**解决**：
1. 确保 `gradle-8.14.3-bin.zip.ok` 文件存在
2. 删除 `mobile/platforms/android/.gradle` 目录
3. 重新运行构建

---

## 📞 需要帮助？

如果以上步骤仍然失败，请提供：
1. PowerShell 执行的完整错误信息
2. `C:\Users\duxia\.gradle\wrapper\dists\gradle-8.14.3-bin\abc123` 目录的截图
3. 构建时的完整日志

---

## ⏭️ 构建成功后

参考文档：
- [构建状态-当前进度.md](./构建状态-当前进度.md)
- [移动端快速启动指南.md](./移动端快速启动指南.md)

